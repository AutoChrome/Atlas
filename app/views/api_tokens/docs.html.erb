<% content_for :title, "API documentation" %>

<% base = request.base_url %>

<div class="stack container--wide">
  <div>
    <p class="text-muted"><%= link_to "← Back to API tokens", api_tokens_path %></p>
    <div class="cluster" style="justify-content: space-between; align-items: flex-start;">
      <h1>API documentation</h1>
      <div class="cluster">
        <%= link_to "Download OpenAPI spec", openapi_api_tokens_path, class: "btn btn--outlined btn--sm" %>
        <%= link_to "Download Bruno collection", bruno_collection_api_tokens_path, class: "btn btn--outlined btn--sm" %>
      </div>
    </div>
    <p class="text-muted">
      A full JSON CRUD API for Areas, Pages, and Announcements, plus a read-only view of
      configured Webhooks, so other applications can read and manage Atlas content directly.
      Authenticated with a bearer token — create one from the
      <%= link_to "API tokens", api_tokens_path %> page. Both downloads above are generated
      from the same endpoint list as this page, so they're never out of sync with it — the
      OpenAPI spec also powers <a href="#notion">connecting Notion AI</a>.
    </p>
  </div>

  <nav class="docs-nav">
    <a href="#authentication" class="btn btn--text btn--sm">Authentication</a>
    <a href="#conventions" class="btn btn--text btn--sm">Conventions</a>
    <a href="#areas" class="btn btn--text btn--sm">Areas</a>
    <a href="#pages" class="btn btn--text btn--sm">Pages</a>
    <a href="#search" class="btn btn--text btn--sm">Search</a>
    <a href="#attachments" class="btn btn--text btn--sm">Inline attachments</a>
    <a href="#announcements" class="btn btn--text btn--sm">Announcements</a>
    <a href="#webhooks" class="btn btn--text btn--sm">Webhooks</a>
    <a href="#scoping" class="btn btn--text btn--sm">Token scoping</a>
    <a href="#notion" class="btn btn--text btn--sm">Connect Notion AI</a>
    <a href="#errors" class="btn btn--text btn--sm">Errors</a>
  </nav>

  <section id="authentication" class="docs-section stack">
    <h2>Authentication</h2>
    <p>
      Every request needs an <code class="font-mono">Authorization</code> header carrying a bearer
      token, generated from the <%= link_to "API tokens", api_tokens_path %> page. Tokens act on
      behalf of the user who created them — audit log entries for anything an API call changes are
      attributed to that user, and the token can only do what that user's role allows (a
      <code class="font-mono">guest</code>-role user's token can read but not write, exactly as in
      the web UI).
    </p>
    <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" <%= base %>/api/v1/areas</pre>
    <p>A missing or invalid token gets a <code class="font-mono">401</code>:</p>
    <pre class="code-block">{"error":"Invalid or missing API token. Pass one as `Authorization: Bearer &lt;token&gt;`."}</pre>
  </section>

  <section id="conventions" class="docs-section stack">
    <h2>Conventions</h2>
    <ul class="stack" style="gap: 8px;">
      <li><strong>Base URL:</strong> <code class="font-mono"><%= base %>/api/v1</code></li>
      <li><strong>Format:</strong> JSON in, JSON out. No need to set <code class="font-mono">Accept</code> or <code class="font-mono">Content-Type</code> — every response under <code class="font-mono">/api</code> is JSON regardless.</li>
      <li><strong>IDs vs slugs:</strong> Areas are addressed by slug in the URL (<code class="font-mono">/api/v1/areas/getting-started</code>); Pages by numeric ID (<code class="font-mono">/api/v1/pages/42</code>) — a page's slug is only unique within its area, so it can't identify a page on its own.</li>
      <li><strong>Pagination:</strong> list endpoints return 25 per page by default, wrapped in a <code class="font-mono">pagination</code> object.</li>
      <li><strong><code class="font-mono">url</code> vs <code class="font-mono">web_url</code>:</strong> every Area/Page response has both — <code class="font-mono">url</code> is this same JSON API endpoint (fetching it returns more JSON), <code class="font-mono">web_url</code> is the actual browsable page.</li>
    </ul>
    <pre class="code-block">{
  "areas": [ { "id": 1, "slug": "getting-started", "name": "Getting Started", ... } ],
  "pagination": { "page": 1, "pages": 3, "count": 61 }
}</pre>
  </section>

  <section id="areas" class="docs-section stack">
    <h2>Areas</h2>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/areas</span>
      </div>
      <p>List areas. A scoped token (see <a href="#scoping">Token scoping</a>) only sees the areas it's been granted.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  "<%= base %>/api/v1/areas?page=1"</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/areas/:slug</span>
      </div>
      <p>Fetch a single area.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/areas/getting-started</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--post">POST</span>
        <span class="font-mono">/api/v1/areas</span>
      </div>
      <p>Create an area. Requires a token with access to <em>all</em> areas — see <a href="#scoping">Token scoping</a>.</p>
      <pre class="code-block">curl -X POST -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "name=Release Notes" \
  -d "description=What shipped, and when." \
  -d "public=true" \
  <%= base %>/api/v1/areas</pre>

      <table class="data-table">
        <thead>
          <tr>
            <th>Parameter</th>
            <th>Description</th>
            <th>Type</th>
            <th>Validation</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="font-mono">name</code></td>
            <td>Display name for the area</td>
            <td>string</td>
            <td><strong>Required</strong> — can't be blank</td>
          </tr>
          <tr>
            <td><code class="font-mono">description</code></td>
            <td>Shown under the area's name in the sidebar and on the area page</td>
            <td>string</td>
            <td>Optional</td>
          </tr>
          <tr>
            <td><code class="font-mono">icon</code></td>
            <td>Font Awesome solid icon name, e.g. <code class="font-mono">rocket</code></td>
            <td>string</td>
            <td>Optional — free text, not checked against the icon set; an unrecognized name just falls back to the default folder icon in the UI</td>
          </tr>
          <tr>
            <td><code class="font-mono">public</code></td>
            <td>Visible without signing in (and makes everything nested under it public too)</td>
            <td>boolean</td>
            <td>Optional — defaults to <code class="font-mono">false</code></td>
          </tr>
          <tr>
            <td><code class="font-mono">position</code></td>
            <td>Sort order among sibling areas</td>
            <td>integer</td>
            <td>Optional — defaults to <code class="font-mono">0</code></td>
          </tr>
          <tr>
            <td><code class="font-mono">parent_id</code></td>
            <td>Nests this area under another</td>
            <td>integer</td>
            <td>Optional — top-level if omitted; must reference an area the token has access to (see <a href="#scoping">Token scoping</a>)</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--patch">PATCH</span>
        <span class="font-mono">/api/v1/areas/:slug</span>
      </div>
      <p>
        Update an area. Same fields as create (above) — all optional to include here since this is
        a partial update, but the same validation applies to any you do send.
      </p>
      <pre class="code-block">curl -X PATCH -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "description=Updated description" \
  <%= base %>/api/v1/areas/getting-started</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--delete">DELETE</span>
        <span class="font-mono">/api/v1/areas/:slug</span>
      </div>
      <p>Delete an area and everything in it. Returns <code class="font-mono">204 No Content</code>.</p>
      <pre class="code-block">curl -X DELETE -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/areas/getting-started</pre>
    </div>
  </section>

  <section id="pages" class="docs-section stack">
    <h2>Pages</h2>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/pages</span>
      </div>
      <p>List pages, optionally filtered to one area. A scoped token only sees pages in areas it's been granted.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  "<%= base %>/api/v1/pages?area_id=1&page=1"</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/pages/:id</span>
      </div>
      <p>Fetch a single page. <code class="font-mono">content</code> comes back as rendered HTML.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/pages/42</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--post">POST</span>
        <span class="font-mono">/api/v1/pages</span>
      </div>
      <p>Create a page. <code class="font-mono">area_id</code> is required, and the token must have access to that area.</p>
      <pre class="code-block">curl -X POST -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "area_id=1" \
  -d "title=Deploying to production" \
  -d "content=&lt;p&gt;Step one: don't panic.&lt;/p&gt;" \
  -d "public=false" \
  <%= base %>/api/v1/pages</pre>

      <table class="data-table">
        <thead>
          <tr>
            <th>Parameter</th>
            <th>Description</th>
            <th>Type</th>
            <th>Validation</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="font-mono">area_id</code></td>
            <td>The area this page belongs to</td>
            <td>integer</td>
            <td><strong>Required</strong> — must reference an existing area the token has access to (see <a href="#scoping">Token scoping</a>)</td>
          </tr>
          <tr>
            <td><code class="font-mono">title</code></td>
            <td>Page title</td>
            <td>string</td>
            <td><strong>Required</strong> — can't be blank</td>
          </tr>
          <tr>
            <td><code class="font-mono">content</code></td>
            <td>Rich text body</td>
            <td>string (HTML)</td>
            <td>Optional</td>
          </tr>
          <tr>
            <td><code class="font-mono">icon</code></td>
            <td>Font Awesome solid icon name, e.g. <code class="font-mono">rocket</code></td>
            <td>string</td>
            <td>Optional — free text, not checked against the icon set</td>
          </tr>
          <tr>
            <td><code class="font-mono">public</code></td>
            <td>Visible without signing in</td>
            <td>boolean</td>
            <td>Optional — defaults to <code class="font-mono">false</code></td>
          </tr>
          <tr>
            <td><code class="font-mono">position</code></td>
            <td>Sort order among sibling pages</td>
            <td>integer</td>
            <td>Optional — defaults to <code class="font-mono">0</code></td>
          </tr>
        </tbody>
      </table>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--patch">PATCH</span>
        <span class="font-mono">/api/v1/pages/:id</span>
      </div>
      <p>
        Update a page. Same fields as create (above), all optional here since it's a partial
        update. Moving it to a different area (by changing <code class="font-mono">area_id</code>)
        requires access to the destination area too.
      </p>
      <pre class="code-block">curl -X PATCH -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "title=Deploying to production (updated)" \
  <%= base %>/api/v1/pages/42</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--delete">DELETE</span>
        <span class="font-mono">/api/v1/pages/:id</span>
      </div>
      <p>Delete a page. Returns <code class="font-mono">204 No Content</code>.</p>
      <pre class="code-block">curl -X DELETE -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/pages/42</pre>
    </div>
  </section>

  <section id="search" class="docs-section stack">
    <h2>Search</h2>
    <p>
      The same index and word-start matching behind the in-app search box, scoped to whatever
      areas the token has access to (see <a href="#scoping">Token scoping</a>) — a scoped token
      never sees an area or page it wasn't granted, even if it matches the search term.
    </p>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/search</span>
      </div>
      <p>Search across areas and pages.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  "<%= base %>/api/v1/search?q=deploy"</pre>

      <table class="data-table">
        <thead>
          <tr>
            <th>Parameter</th>
            <th>Description</th>
            <th>Type</th>
            <th>Validation</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="font-mono">q</code></td>
            <td>The search term</td>
            <td>string</td>
            <td>Required — blank or missing returns an empty <code class="font-mono">results</code> list rather than an error</td>
          </tr>
          <tr>
            <td><code class="font-mono">areas</code></td>
            <td>Include areas in the results</td>
            <td>boolean</td>
            <td>Optional — defaults to <code class="font-mono">true</code></td>
          </tr>
          <tr>
            <td><code class="font-mono">pages</code></td>
            <td>Include pages in the results</td>
            <td>boolean</td>
            <td>Optional — defaults to <code class="font-mono">true</code>. If <code class="font-mono">areas</code> and <code class="font-mono">pages</code> are both <code class="font-mono">false</code>, the result is always an empty list.</td>
          </tr>
        </tbody>
      </table>

      <pre class="code-block">{
  "results": [
    {
      "type": "Area",
      "id": 3,
      "slug": "deployments",
      "title": "Deployments",
      "public": false,
      "url": "<%= base %>/api/v1/areas/deployments",
      "web_url": "<%= base %>/areas/deployments"
    },
    {
      "type": "Page",
      "id": 42,
      "title": "Deploying to production",
      "area": "Engineering",
      "public": false,
      "url": "<%= base %>/api/v1/pages/42",
      "web_url": "<%= base %>/areas/engineering/pages/deploying-to-production"
    }
  ]
}</pre>
      <p class="field__hint">
        Each result carries its own <code class="font-mono">type</code> (<code class="font-mono">"Area"</code>
        or <code class="font-mono">"Page"</code>) since the two are otherwise shaped alike here — fetch
        <code class="font-mono">url</code> for the full record from the matching endpoint above.
      </p>
    </div>
  </section>

  <section id="attachments" class="docs-section stack">
    <h2>Inline attachments</h2>
    <p>
      A page's <code class="font-mono">content</code> is plain HTML, so text-only pages need nothing
      special — but embedding an image or file <em>inline</em> (the way dragging one into the Trix
      editor does in the web UI) takes two steps: upload it to get a reference, then splice that
      reference into the HTML you send as <code class="font-mono">content</code>. It's the same
      mechanism Trix itself uses under the hood.
    </p>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--post">POST</span>
        <span class="font-mono">/api/v1/attachments</span>
      </div>
      <p>
        Upload a file. Not tied to any page yet — just registers the file and hands back an
        <code class="font-mono">html</code> snippet ready to embed.
      </p>

      <table class="data-table">
        <thead>
          <tr>
            <th>Parameter</th>
            <th>Description</th>
            <th>Type</th>
            <th>Validation</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="font-mono">file</code></td>
            <td>The file to upload</td>
            <td>file (<code class="font-mono">multipart/form-data</code>)</td>
            <td><strong>Required</strong></td>
          </tr>
        </tbody>
      </table>

      <pre class="code-block">curl -X POST -H "Authorization: Bearer YOUR_API_TOKEN" \
  -F "file=@screenshot.png;type=image/png" \
  <%= base %>/api/v1/attachments</pre>
      <pre class="code-block">{
  "signed_id": "eyJfcmFpbHMiOns...",
  "filename": "screenshot.png",
  "content_type": "image/png",
  "byte_size": 48213,
  "html": "&lt;action-text-attachment sgid=\"...\" content-type=\"image/png\" filename=\"screenshot.png\" filesize=\"48213\" previewable=\"true\"&gt;&lt;/action-text-attachment&gt;"
}</pre>
    </div>

    <div>
      <p>
        Paste that <code class="font-mono">html</code> value directly into the <code class="font-mono">content</code>
        you send when creating or updating the page — anywhere in the string, it'll render at that
        point (an inline image for image types, a download card for anything else):
      </p>
      <pre class="code-block">curl -X POST -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "area_id=1" \
  -d "title=Deploy screenshots" \
  -d "content=&lt;p&gt;Here's the dashboard after deploy:&lt;/p&gt;&lt;action-text-attachment sgid=\"...\" content-type=\"image/png\" filename=\"screenshot.png\" filesize=\"48213\" previewable=\"true\"&gt;&lt;/action-text-attachment&gt;" \
  <%= base %>/api/v1/pages</pre>
      <p class="field__hint">
        Don't hand-write the <code class="font-mono">&lt;action-text-attachment&gt;</code> tag yourself —
        use the exact <code class="font-mono">html</code> the upload call returned. Its internal
        format isn't a stable public contract; the upload response is.
      </p>
    </div>
  </section>

  <section id="announcements" class="docs-section stack">
    <h2>Announcements</h2>
    <p>
      Unlike Areas and Pages, announcements aren't scoped to a token's areas — any token can read
      them, and any token belonging to an admin or member can create, update, or delete them.
      Creating or updating one through the API never publishes it or sends anything to a webhook;
      publishing stays a deliberate, separate action taken from the Atlas web UI, to keep a typo
      from immediately going out everywhere.
    </p>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/announcements</span>
      </div>
      <p>List announcements, most recently created first.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  "<%= base %>/api/v1/announcements?page=1"</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/announcements/:id</span>
      </div>
      <p>Fetch a single announcement.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/announcements/42</pre>
      <pre class="code-block">{
  "id": 42,
  "title": "Scheduled maintenance this weekend",
  "content_html": "&lt;div&gt;&lt;p&gt;We'll be taking the app down...&lt;/p&gt;&lt;/div&gt;",
  "author": "Jordan Blake",
  "published_at": "2026-09-05T18:32:10Z",
  "starts_on": "2026-09-06",
  "ends_on": "2026-09-07",
  "created_at": "2026-09-05T18:30:00Z",
  "updated_at": "2026-09-05T18:32:10Z",
  "url": "<%= base %>/api/v1/announcements/42",
  "web_url": "<%= base %>/announcements/42"
}</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--post">POST</span>
        <span class="font-mono">/api/v1/announcements</span>
      </div>
      <p>Create an announcement, as a draft.</p>
      <pre class="code-block">curl -X POST -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "title=Scheduled maintenance this weekend" \
  -d "content=&lt;p&gt;We'll be taking the app down...&lt;/p&gt;" \
  -d "starts_on=2026-09-06" \
  -d "ends_on=2026-09-07" \
  <%= base %>/api/v1/announcements</pre>

      <table class="data-table">
        <thead>
          <tr>
            <th>Parameter</th>
            <th>Description</th>
            <th>Type</th>
            <th>Validation</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><code class="font-mono">title</code></td>
            <td>Headline shown at the top of the announcement</td>
            <td>string</td>
            <td><strong>Required</strong> — can't be blank</td>
          </tr>
          <tr>
            <td><code class="font-mono">content</code></td>
            <td>Rich text body</td>
            <td>string (HTML)</td>
            <td>Optional</td>
          </tr>
          <tr>
            <td><code class="font-mono">starts_on</code></td>
            <td>First day the announcement is live</td>
            <td>date (<code class="font-mono">YYYY-MM-DD</code>)</td>
            <td><strong>Required</strong> — must be a real, parseable date. No time component — announcements run whole days.</td>
          </tr>
          <tr>
            <td><code class="font-mono">ends_on</code></td>
            <td>Last day the announcement is live (inclusive)</td>
            <td>date (<code class="font-mono">YYYY-MM-DD</code>)</td>
            <td><strong>Required</strong> — must be a real date, and can't be before <code class="font-mono">starts_on</code>. The same date as <code class="font-mono">starts_on</code> is valid — that's a one-day announcement.</td>
          </tr>
        </tbody>
      </table>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--patch">PATCH</span>
        <span class="font-mono">/api/v1/announcements/:id</span>
      </div>
      <p>
        Update an announcement. Same fields as create (above) — optional to include here since
        it's a partial update, but the same validation applies to any you do send, and you can't
        clear a required field (<code class="font-mono">title</code>, <code class="font-mono">starts_on</code>,
        <code class="font-mono">ends_on</code>) by sending it blank.
      </p>
      <pre class="code-block">curl -X PATCH -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "ends_on=2026-09-08" \
  <%= base %>/api/v1/announcements/42</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--delete">DELETE</span>
        <span class="font-mono">/api/v1/announcements/:id</span>
      </div>
      <p>Delete an announcement. Returns <code class="font-mono">204 No Content</code>.</p>
      <pre class="code-block">curl -X DELETE -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/announcements/42</pre>
    </div>
  </section>

  <section id="webhooks" class="docs-section stack">
    <h2>Webhooks</h2>
    <p>
      Read-only, and restricted to tokens belonging to an <strong>admin</strong> — same trust
      boundary as managing webhooks in the web UI. The signing secret is never included in API
      responses; see it from the webhook's edit page in Atlas instead. To learn how deliveries
      themselves work — payload shape, signature verification — see the
      <% if current_user&.admin? %>
        <%= link_to "webhook integration guide", docs_admin_webhooks_path %>.
      <% else %>
        webhook integration guide (admin only).
      <% end %>
    </p>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/webhooks</span>
      </div>
      <p>List configured webhooks.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  "<%= base %>/api/v1/webhooks?page=1"</pre>
      <pre class="code-block">{
  "webhooks": [
    { "id": 1, "description": "Ops Slack relay", "url": "https://...", "active": true, ... }
  ],
  "pagination": { "page": 1, "pages": 1, "count": 1 }
}</pre>
    </div>

    <div>
      <div class="docs-endpoint">
        <span class="docs-endpoint__method docs-endpoint__method--get">GET</span>
        <span class="font-mono">/api/v1/webhooks/:id</span>
      </div>
      <p>Fetch a single webhook.</p>
      <pre class="code-block">curl -H "Authorization: Bearer YOUR_API_TOKEN" \
  <%= base %>/api/v1/webhooks/1</pre>
    </div>
  </section>

  <section id="notion" class="docs-section stack">
    <h2>Connect Notion AI</h2>
    <p>
      Notion AI's custom connectors call an external API on your behalf once you give it an
      <a href="https://swagger.io/specification/">OpenAPI</a> spec describing that API — that's
      exactly what <%= link_to "the OpenAPI spec above", openapi_api_tokens_path %> is for. Once
      connected, asking Notion AI to draft or update something can create or edit the matching
      Atlas page directly, without anyone copying content across by hand.
    </p>

    <ol style="display: flex; flex-direction: column; gap: 12px; padding-left: 20px;">
      <li>
        Create an API token from the <%= link_to "API tokens", api_tokens_path %> page. Give it
        access to <strong>all areas</strong> if Notion should be able to create pages anywhere,
        or scope it to specific areas to limit it to those — see
        <a href="#scoping">Token scoping</a>. It needs to belong to an admin or member to create
        or edit anything (a guest-role token can only read).
      </li>
      <li>
        Download <%= link_to "the OpenAPI spec", openapi_api_tokens_path %>.
      </li>
      <li>
        In Notion, go to <strong>Settings → Connections → Custom Connector</strong> (or wherever
        your workspace's AI connector setup lives — Notion has moved this before) and upload the
        spec file.
      </li>
      <li>
        When Notion asks how to authenticate, choose <strong>Bearer token</strong> and paste in
        the API token from step 1. Nothing else needs configuring — the spec already describes
        every endpoint, parameter, and requirement.
      </li>
      <li>
        Ask Notion AI to use it: <em>"Using the Atlas connector, create a page in the Engineering
        area titled 'Deploying to production' with these steps: ..."</em> Notion AI fills in the
        request from the spec and your prompt — <code class="font-mono">content</code> arrives at
        Atlas as HTML either way, whether Notion sends rendered HTML or Notion-flavored markdown
        it converts first.
      </li>
    </ol>

    <p class="field__hint">
      The spec is regenerated fresh on every download, from the same source as the rest of this
      page — if the API changes, just re-download and re-upload it to Notion; nothing about the
      connector's setup (the token, the auth choice) needs to change.
    </p>
  </section>

  <section id="scoping" class="docs-section stack">
    <h2>Token scoping</h2>
    <p>
      Every token has either access to <strong>all areas</strong>, or to a specific list of areas
      chosen when it's created (on the <%= link_to "API tokens", api_tokens_path %> page — scope
      can't be changed after creation; revoke and issue a new token instead). A page's area
      determines access to that page too — there's no separate per-page scope.
    </p>
    <p>
      Only an all-areas token can create new top-level areas: a brand-new area has no existing
      scope to check it against, so scoped tokens are limited to reading and managing the areas
      (and pages within them) they've already been granted.
    </p>
    <p>Trying to touch an area — or a page inside an area — the token wasn't granted returns a <code class="font-mono">403</code>:</p>
    <pre class="code-block">curl -X PATCH -H "Authorization: Bearer YOUR_API_TOKEN" \
  -d "name=Renamed" \
  <%= base %>/api/v1/areas/some-other-team-area

{"error":"This API token doesn't have access to the \"Some Other Team Area\" area."}</pre>
  </section>

  <section id="errors" class="docs-section stack">
    <h2>Errors</h2>
    <table class="data-table">
      <thead>
        <tr>
          <th>Status</th>
          <th>Meaning</th>
        </tr>
      </thead>
      <tbody>
        <tr><td><code class="font-mono">401</code></td><td>Missing or invalid token.</td></tr>
        <tr><td><code class="font-mono">403</code></td><td>Valid token, but not allowed — wrong role, or that area isn't in the token's scope.</td></tr>
        <tr><td><code class="font-mono">404</code></td><td>No area/page matches that slug or ID.</td></tr>
        <tr><td><code class="font-mono">422</code></td><td>Validation failed — the response body has an <code class="font-mono">errors</code> array.</td></tr>
      </tbody>
    </table>
    <pre class="code-block">{"errors":["Title can't be blank"]}</pre>
  </section>
</div>
